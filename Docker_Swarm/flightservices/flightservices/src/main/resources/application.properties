spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/reservation
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Sagar@123}
spring.jpa.show-sql=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.flyway.enabled=true
spring.flyway.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/reservation
spring.flyway.user=${DB_USERNAME:root}
spring.flyway.password=${DB_PASSWORD:Sagar@123}
spring.flyway.baseline-on-migrate=true

